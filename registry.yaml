---
version: 0.1
fields:
  - table: posts
    column: author_email
    type: varchar(255)
    placeholder: "foo@example.com"
    validator: "email"
  - table: posts
    column: rating
    type: int
    placeholder: "5"
    validator: "number"
